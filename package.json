{
  "name": "traza-publica-landing",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "dev:docker": "rm -f .astro/dev.json && astro dev --host --force",
    "build": "astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^7.0.2"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
